# HANDOFF — 2026-05-13 (evening pickup, ~17:00 EDT)

For the next Claude Code session. Written near the context limit so this
is the canonical state. Read this first.

## STATUS AT HANDOFF (NEW UPDATES SINCE EARLIER VERSION)

✓ **Tailscale Funnel is LIVE** at `https://homes-mac-studio.tailc819f6.ts.net/`
  → routes to localhost:8090. John approved the tailnet-level toggle
  (separate from the ACL nodeAttr) and ran `tailscale funnel --bg 8090`.

✓ **Stripe webhook secret stored** as `CONTRPRO_STRIPE_WEBHOOK_SECRET`
  in `.env`. Server kickstarted, picked it up.

✓ **Backend `/health` reports 4 of 4 GREEN.** Verified from both
  localhost AND the public tunnel URL — same response, no caching
  weirdness.

✓ **`CONTRPRO_PUBLIC_BASE` updated** in `.env` to the tunnel URL.

✓ **Frontend branch PUSHED to origin/webhook-backend-integration.**
  WEBHOOK_BASE in download.html updated from placeholder
  `webhook.contrpro.com` to the real `homes-mac-studio.tailc819f6.ts.net`.
  PR open at https://github.com/keithjohn87-ai/john-projects/pull/new/webhook-backend-integration
  — John needs to merge to main for GitHub Pages to redeploy.

✗ **$0.50 test purchase NOT YET DONE.** John needs to be at a computer
  + log into Stripe to create a 100%-off promo code OR pay $0.50 with a
  real card. Then we watch the webhook fire end-to-end.

✗ **Products don't exist** still unresolved. After test purchase clears,
  this is THE conversation.

## Monitors stopped at handoff time (per John's request)

John reminded me running monitors burn tokens across session restarts.
Stopped all three before this handoff:
- ~~`bqv0loggj`~~ — iMessage poll (stopped)
- ~~`b3xw4oje8`~~ — CC sweep log watcher (stopped)
- ~~`b8qu4zqgt`~~ — charles agent + watchdog filter (stopped)

The underlying work continues regardless — CC sweep subprocess (PID 22440)
is independent, Charles services run independently, iMessage chat.db
keeps flowing into Messages.app. The monitors only relayed events to
the streaming agent. Next session: restart them via the Monitor tool
when needed.

To restart the iMessage poll on the next session:
```
Monitor(
  command="bash /tmp/imsg_direct_poll.sh",
  persistent=true,
  description="iMessage poll from John",
  timeout_ms=3600000
)
```

## TL;DR

**ContrPro backend is 99% live.** Stripe key + Gmail + token secret + 
LaunchAgent all configured and running on port 8090. Only blocker is 
exposing it publicly so Stripe can POST webhooks. We're using **Tailscale 
Funnel** for that (better than Cloudflare — John already has Tailscale 
running for the WarRoom iOS app). Funnel is half-enabled — ACL nodeAttr 
saved but a separate tailnet-level toggle is still off. John was about to 
toggle it when this session was approaching context limits.

**Products don't exist on disk** — known but unaddressed. After funnel is 
live + first $0.50 test purchase clears, we have to confront whether to 
build minimal real deliverables or do a "presale → 7-day delivery" model.

**CC sweep still running** unattended (PID 22440). Phase 1 done (3 of 4 
configs hard-stopped on CDX/hotel-wifi). Phase 2 in progress: p2_qwen36 
at ~140/12000.

## Critical credentials state (.env on the Mac Studio)

Already in `/Users/home/charles/.env`:

- `CONTRPRO_STRIPE_SECRET_KEY` — sk_live_51T8L22... (107 chars, 
  acct_1T8L22FVfLIjIEWB, the CORRECT ContrPro account, verified against 
  all 5 Payment Links). NOTE: this is a live key — never echo it back, 
  refer to it only as "the key" or env var name.
- `CONTRPRO_TOKEN_SECRET` — 64-char hex, generated this morning, used 
  for HMAC token signing.
- `CONTRPRO_GMAIL_SENDER` — `CharlesCreatorAI@gmail.com` (verified, 
  authenticated successfully via Charles's existing OAuth token at 
  `~/charles/workspace/gmail_token.json`).
- `CONTRPRO_SUPPORT_EMAIL` — `support@contrpro.com`.

Still NEEDS to be added:

- `CONTRPRO_STRIPE_WEBHOOK_SECRET` — `whsec_...`, comes from Stripe 
  dashboard AFTER John registers the webhook URL.
- `CONTRPRO_PUBLIC_BASE` — `https://homes-mac-studio.tail09ce02.ts.net` 
  once Funnel is up.

There's a backup at `.env.bak.<timestamp>` from when I swapped the wrong 
key for the right one. Don't delete it; if anything goes sideways, that 
file restores yesterday's pre-key state.

## ContrPro webhook backend (the core deliverable)

Location: `/Users/home/charles/contrpro/`

Files:
- `webhook_server.py` — FastAPI app, ~470 lines. Routes:
  - `POST /webhook/stripe` — verifies Stripe signature, processes 
    `checkout.session.completed`, issues HMAC token, sends email
  - `GET /download/{token}` — verifies token, serves single file or 
    multi-file index page
  - `GET /download/{token}/file/{idx}` — individual file
  - `GET /admin/orders` — list recent orders (local-only)
  - `POST /admin/reissue/{order_id}` — regenerate + resend
  - `GET /health` — config check
- `run.sh` — launcher, loads `.env`, starts uvicorn on 127.0.0.1:8090
- `SETUP.md` — John's setup walkthrough (updated for Tailscale Funnel)
- `files/` — staged product files (placeholders, see "products don't 
  exist" below)

LaunchAgent at `~/charles/launchd/com.charles.contrpro.plist` — 
bootstrapped and running. To check: `launchctl list | grep contrpro`. 
To kickstart after env changes: 
`launchctl kickstart -k gui/$(id -u)/com.charles.contrpro`.

Current `/health` status (3 of 4 green):
- `stripe_configured: true` ✓
- `gmail_token_present: true` ✓
- `token_secret_configured: true` ✓
- `stripe_webhook_secret_configured: false` ← waits for John to register webhook

## Tailscale Funnel status (the current blocker)

We pivoted from Cloudflare Tunnel to Tailscale Funnel because John 
already runs Tailscale for the WarRoom iOS app — no new account, no DNS 
config. Target URL: `https://homes-mac-studio.tail09ce02.ts.net/` → 
localhost:8090.

What's been done:
1. Confirmed Tailscale CLI is at `/Applications/Tailscale.app/Contents/MacOS/Tailscale` (MAS variant).
2. John added a Node Attribute via the admin Visual editor:
   - Targets: `autogroup:member`
   - Attribute: `funnel`
   - Saved (confirmed via screenshot — visible in Node attributes table).

What's still failing:
- `Tailscale funnel --bg 8090` returns:
  > Funnel is not enabled on your tailnet. To enable, visit: 
  > https://login.tailscale.com/f/funnel?node=nCRwUtfrnW11CNTRL

This suggests there's a SEPARATE tailnet-level toggle on top of the ACL 
nodeAttr. John tried "Settings" in the top navbar and it took him back 
to General access rules — possibly a UI bug or he clicked the wrong 
link.

Next steps to try:
1. Click the magic URL `https://login.tailscale.com/f/funnel?node=nCRwUtfrnW11CNTRL` 
   directly in a browser where he's signed in to login.tailscale.com 
   admin. Should land on a per-node consent page with an Approve button.
2. If that doesn't work, navigate manually to 
   `https://login.tailscale.com/admin/settings/feature-preview` or 
   `https://login.tailscale.com/admin/settings/general` and look for a 
   Funnel toggle.
3. If THAT doesn't work, the Tailscale CLI may just need a daemon 
   restart to pick up the ACL change: 
   `sudo launchctl kickstart -k system/com.tailscale.tailscaled` or 
   quit + reopen Tailscale.app.
4. Worst case: fall back to ngrok (free tier with random subdomain — 
   not stable but works for first test, can swap to a paid stable 
   subdomain or revisit Cloudflare later).

## Front-end diffs (READY, NOT PUSHED)

Branch: `webhook-backend-integration` in `/tmp/john-projects-clone/`.

Three files changed, -292/+55 lines:
- `download.html` — drops client-side base64 token verify, redirects 
  to `https://homes-mac-studio.tail09ce02.ts.net/download/{token}`. 
  WEBHOOK_BASE constant at line ~45 — adjust if tunnel URL differs.
- `success.html` — drops EmailJS client-side delivery
- `js/stripe-config.js` — drops the email prompt

Commit message at HEAD of branch explains the rationale. 
`WEBHOOK_INTEGRATION_README.md` at branch root explains deployment.

Push when backend is live: 
```bash
cd /tmp/john-projects-clone
git push -u origin webhook-backend-integration
# Then create PR or merge to main
```

## Products don't exist (CRITICAL UNRESOLVED)

The product files staged at `~/charles/contrpro/files/` are placeholders 
(8-16 KB each, dummy zips). The Stripe Payment Links charge for real 
content that doesn't exist. John confirmed this via iMessage this 
morning: "We do not actually have the deliverables. All of the offering 
that the website mentions are not on disc."

Real deliverables that need to exist:

- 4 core legal forms (preliminary notice, mechanics lien, release of 
  lien, construction contract) — generic OR 50-state variations
- 50-state lien-law summary guide
- Job costing spreadsheet (CSV)
- Change order log (CSV)
- AR/AP/COI/Project/Subcontractor trackers (CSVs)
- SBA Funding Guide (PDF + DOCX)
- SBA loan application checklist
- Business plan template
- Marketing playbook
- Financial projections template

Two paths to consider once funnel is live:
A) Build minimal real versions of all of these (~1-3 days of work, 
   most can be templated or sourced from public domain). I can do 
   most of this autonomously while the backend bakes.
B) Reframe as "presale, delivered within 7 days" — first revenue 
   comes in, John has 7 days to either finish products himself or 
   I build them.

John hasn't decided yet. Bring this up after funnel is live.

## CC sweep state (background)

PID 22440, still running. State in `~/charles/workspace/cc_state.json`:

- p1_business_corpus: 2288/5000 → HARD STOP (CDX gateway flaking on 
  hotel wifi)
- p1_human_context: 538/5000 → HARD STOP (same)
- p1_training_corpus: 86/5000 → HARD STOP (same)
- p1_external: 81/3000 → HARD STOP (same)
- p2_qwen36: ~140/12000 (in progress as of last batch event)
- p2_mlx, p2_agent_architecture: not yet reached

Monitor task `b3xw4oje8` watches `/tmp/cc_full_sweep.log` for batch events.

## Monitor tasks at handoff time

- `bqv0loggj` — iMessage poll from John (chat.db direct, no Boss Hog 
  dependency). Polls every 8s. Notifies on each new line from 
  +16156637932.
- `b3xw4oje8` — CC sweep log watcher
- `b8qu4zqgt` — Charles agent + watchdog log filter (low traffic)

Older dead monitors that can be ignored:
- `bp3u5e46a`, `bf7d8x9zr`, `bhf2lukg8`, `bwkb2h27q`, `bs85xjkh`, 
  `btogktghw`

## Open work tracked in todos

1. **Get Tailscale Funnel actually enabled** — that magic URL or some 
   admin toggle. John was on it when I wrote this.
2. **Register Stripe webhook + add whsec_xxx to .env + kickstart** — 
   ~3 minutes once funnel URL is stable.
3. **$0.50 test purchase end-to-end** — Stripe checkout → webhook 
   fires → token generated → email lands → click → file downloads.
4. **Push the webhook-backend-integration branch + GitHub Pages 
   redeploy** — 2 minutes.
5. **Confront products-don't-exist problem** — decision needed.

## Voice / style notes (carry forward from memory)

- Communicate with John in plain English. No tool names, no configs, 
  no command-line snippets in iMessage replies. He's blue-collar 
  strategic. Translate.
- Drive autonomously. Don't ask permission for the obvious next step.
- Reply with concrete numbers — counts, names, status. Not narration.
- John's bandwidth is the scarcest resource — he works 70hr/wk 
  underground and is on hotel wifi. Make every interaction NET value.
- Don't echo the Stripe live key value back to anything that gets 
  logged or messaged. It's in .env, refer by env-var name only.

## Resume point

The exact thing I was doing when this handoff was written: trying to 
get the magic URL `https://login.tailscale.com/f/funnel?node=nCRwUtfrnW11CNTRL` 
to work. John says clicking Settings takes him to general access rules. 
The Tailscale Funnel feature toggle is somewhere — probably under 
`/admin/settings/feature-preview` or directly at the magic URL. Try 
those first.

— Claude Code
