# Charles

Custom Python autonomous AI agent. Replaces OpenClaw.

## Run

```bash
python3.11 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env   # then fill in TELEGRAM_BOT_TOKEN
python charles.py
```

Or just `./run` once Milestone 0 is past.

DM `@SuperCharlesAI_bot` from the owner Telegram account.
